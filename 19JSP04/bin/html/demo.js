var a,b;
function msg() {
       a = prompt("input: ");
       v = prompt("input: ");
       var s = parstInt(a) + parstInt(b);
       if (s % 2 == 0) {
       	alert("so chan");
       }
       else{
       		alert("so le");
       }      	
    }
